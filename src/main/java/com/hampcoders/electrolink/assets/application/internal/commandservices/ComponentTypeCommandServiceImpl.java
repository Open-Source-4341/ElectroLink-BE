package com.hampcoders.electrolink.assets.application.internal.commandservices;

public class ComponentTypeCommandServiceImpl {
}

/*
* public void handleAddPhoto(UUID propertyId, String photoUrl) {
        if (propertyId.equals(this.id))
            setPhoto(photoUrl);
    }

    public void handleUpdateAddress(UUID propertyId, Address newAddress) {
        if (propertyId.equals(this.id))
            updateAddress(newAddress);
    }

    public void handleDeactivate(UUID propertyId) {
        if (propertyId.equals(this.id))
            deactivate();
    }
* */